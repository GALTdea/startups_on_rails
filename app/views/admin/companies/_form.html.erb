<% if current_user.admin? %>
  <div class="field">
    <%= f.label :user_id, "Company Owner" %>
    <%= f.collection_select :user_id, @users, :id, :email, 
          { include_blank: "Select Owner (optional)" },
          class: "form-select" %>
  </div>
<% end %> 